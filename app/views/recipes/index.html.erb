<h1>Current Pantry</h1>
<div id="CurrentPantry">

</div>
<%= form_tag "/recipes", method: "GET" do %>

<% end %>
<hr/>
<div id="SearchResults">
    <% @recipes.each do |recipe| %>
      <div class="RecipeCard">
        <%= link_to image_tag(recipe.image), "/recipes/" + recipe.id.to_s %>
        <%= recipe.name %>
        <%= recipe.preptime %>
        <%= recipe.cooktime %>
        <%= recipe.serving %>
      </div>
    <% end %>
<div>
